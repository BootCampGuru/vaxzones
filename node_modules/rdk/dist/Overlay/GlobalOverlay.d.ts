import { FC } from 'react';
import { GlobalOverlayCommon } from './useGlobalOverlay';
export interface GlobalOverlayProps extends GlobalOverlayCommon {
    open: boolean;
    children?: any;
    render?: (props: {
        overlayIndex: number;
    }) => void;
    onClose?: () => void;
}
export declare const GlobalOverlay: FC<GlobalOverlayProps>;
